% =========================================================
%                CV HEADER.TEX (CLEAN VERSION)
% =========================================================

% ---------- Font / Engine ----------
\usepackage{fontspec}
\setmainfont{Times New Roman}

% ---------- Micro-typography ----------
\usepackage{microtype}
\usepackage{ragged2e}
\usepackage{setspace}

% ---------- Colors ----------
\usepackage{xcolor}
\definecolor{cvblue}{HTML}{1F4E79}

% ---------- Icons ----------
\usepackage{fontawesome5}
\newcommand{\icon}[1]{\textcolor{cvblue}{#1}}

% ---------- Layout ----------
\setlength{\parindent}{0pt}
\setlength{\parskip}{2pt}

% Two-column spacing (if ever used)
\setlength{\columnsep}{16pt}
\setlength{\columnseprule}{0pt}

% ---------- Lists (tight CV style) ----------
\usepackage{enumitem}
\setlist[itemize]{leftmargin=*, noitemsep, topsep=2pt}
\setlist[enumerate]{leftmargin=*, noitemsep, topsep=2pt}

% ---------- Section formatting ----------
\usepackage{titlesec}

\titleformat{\section}
  {\centering\normalfont\Large\bfseries}
  {}
  {0pt}
  {}

\titlespacing*{\section}
  {0pt}
  {0.4\baselineskip}
  {0.2\baselineskip}

\titleformat{\subsection}
  {\centering\normalfont\large\bfseries}
  {}
  {0pt}
  {}

\titlespacing*{\subsection}
  {0pt}
  {0.3\baselineskip}
  {0.15\baselineskip}

% ---------- Title tightening ----------
\usepackage{titling}

\pretitle{\begin{center}\Huge\bfseries}
\posttitle{\par\end{center}\vspace{-0.5em}}

\predate{\begin{center}}
\postdate{\par\end{center}\vspace{-1em}}

% ---------- Links ----------
\usepackage[hidelinks]{hyperref}
\hypersetup{
  colorlinks=true,
  urlcolor=cvblue
}

% ---------- Clean page ----------
\pagestyle{empty}
